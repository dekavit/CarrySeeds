using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerItems : MonoBehaviour
{
    public int speedup,drill;
    // Start is called before the first frame update
    void Start()
    {
        speedup = 0;
        drill = 0;
    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log(speedup);
    }
}
